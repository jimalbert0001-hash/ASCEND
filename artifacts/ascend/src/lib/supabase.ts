// Supabase has been replaced by Replit Auth (session-based).
// This file is kept to avoid import errors from files that may still reference it.
export const isSupabaseConfigured = false;
export const supabase = null as any;
